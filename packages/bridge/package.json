{
  "name": "@prime-pocket/bridge",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "prime-pocket": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/cli.ts bridge",
    "start": "node dist/cli.js bridge",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts src/**/**/*.test.ts",
    "e2e": "node --import tsx src/e2e-smoke.ts"
  },
  "dependencies": {
    "@prime-pocket/protocol": "workspace:*",
    "commander": "^12.1.0",
    "qrcode-terminal": "^0.12.0",
    "selfsigned": "^2.4.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/qrcode-terminal": "^0.12.2",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
